// public/reform-wire.js
// Provide a global so code that expects it won't crash.
// Fill with real wiring later.
window.ReformWire = window.ReformWire || {
  version: 'stub-1',
  ready: true,
  // add functions or config your app expects here
};
