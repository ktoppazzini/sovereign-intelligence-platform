// This file is server by default (no 'use client'), so metadata is allowed.
export const metadata = {
  title: 'Translation Playground',
};

export default function TranslationLayout({ children }) {
  return children;
}
