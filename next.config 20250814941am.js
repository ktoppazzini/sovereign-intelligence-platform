// next.config.js (ESM)
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Add custom config here only if you need it:
  // images: { domains: ['example.com'] },
  // experimental: { serverActions: { bodySizeLimit: '2mb' } },
};

export default nextConfig;
